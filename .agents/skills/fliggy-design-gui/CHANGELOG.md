# Changelog

### v0.3.4

| 维度 | 变更 |
|------|------|
| **tabs.home** | 示例页移除金刚下大促活动条（`home-campaign-*`）；[`pages/tabs/home/spec.md`](pages/tabs/home/spec.md) 同步删模块顺序、间距、生成纪律与一览表中的大促表述。 |
| **编排 Skill** | YAML `version`：`0.3.3` → `0.3.4`；意图表与「A 类 · tabs.home」节去掉 `home-campaign-*` 硬规则，改为金刚下直接衔接频道 + Feeds 及局部改动示例。 |

### v0.3.3

| 维度 | 变更 |
|------|------|
| **真实数据** | 新增「真实数据追问可见性」硬规则：flyai 可用时须在用户可见正文单独输出标准问句，禁止仅写在思考链；允许跳过须写明理由。与设计审查摘要强制对齐登记「真实数据追问：…」。审查清单 4.1 增加对应 Key 项。 |
| **版本** | YAML `version`：`0.3.1` → `0.3.3`；真实数据依赖条补充 flyai 安装链接。 |

### v0.3.2

| 维度 | 变更 |
|------|------|
| **A/B 默认流程** | 新增「A/B 拷贝优先总规则」：默认先整页拷贝基线 `example.html`，再做局部改动；未点名区块不得重写。 |
| **锚点约定** | 新增可选注释锚点 `FDG_EDIT_START/END` 规范，限定可编辑区，降低模型误改风险。 |
| **审查门禁** | DoD 第 6 条从「意图 A 且整页基线」扩展为「意图 A/B 且涉及基线 example」，统一要求摘要列明未改区块与实际改动。 |
| **版本** | 规则增强：`0.3.1` → `0.3.2`。 |

### v0.3.1

| 维度 | 变更 |
|------|------|
| **意图 A · tabs.home** | 新增专节「整页拷贝再改」：禁止凭记忆重画顶栏/固定搜索/金刚/底栏/频道/Feeds；大促优先改 `home-campaign-*`；须声明未改区块与衔接 channel+Feeds。 |
| **意图表** | 新增 `tabs.home` 金刚下大促类表述一行，指向下节硬规则。 |
| **读取顺序** | 共性前置后增加过渡句；A/B 分支从 4 起连续编号；C 分支去掉与「三条硬纪律」重复的步骤 7。 |
| **输出与纪律** | 结语文禁：用「完成」等结语必须同条带完整审查摘要；站外 `*.html` 不得跳过审查与基线对照。 |
| **设计审查** | DoD 第 6 条：意图 A 整页基线须在摘要中列明未改/已改区块；4.2 清单新增「未动区块一致（tabs.home）」Key 项；摘要模板增加两行字段；补充结论取值一句话定义。 |
| **版本** | YAML `version`：`0.3.0` → `0.3.1`。 |

### v0.3.0

| 维度 | 变更 |
|------|------|
| **边界节** | 移除独立「与 fliggy-design-skill 的边界」节，暂不维护显式边界说明。 |
| **版本号** | 新增两个版本号的语义区分说明（Skill 编排版本 vs 设计基准版本，独立演进）。 |
| **意图识别** | 将原独立「启动问答」章节合并为「意图识别」的子节「启动问答格式」，消除重复。 |
| **意图示例** | 意图分流表新增搜索三场景（各独立一行：`search.search` / `search.search_sug` / `search.search_result`），精确对应各自 `example.html` + `spec.md` 路径。 |
| **读取顺序** | 修复 A/B 与 C 分支步骤编号与共性前置冲突（原均从「4.」开始，现统一从「5.」延续）。 |
| **设计审查** | 新增「设计审查（/design-review）」整节；A/B/C 分流清单；`quick` / `standard` / `full` 深度；摘要模板与修复闭环；补充 `PASS` / `PASS_WITH_NOTES` / `FAIL` 三种结论的区别定义；Key 清零门禁。 |
| **飞猪真实数据** | `flyai skill` 真实数据能力依赖项添加安装入口链接。 |
| **同步机制** | 移除 `scripts/sync-skill.sh`、`scripts/launchd/` 定时示例 plist、根目录 `Makefile`（`make sync-skill`）；技能更新改为手动 `git pull` 后重新链接/复制。 |
| **版本** | YAML `version`：`0.2.8` → `0.3.0`（`spec_version` 仍以 foundations **0.2.8** 为准）。 |
