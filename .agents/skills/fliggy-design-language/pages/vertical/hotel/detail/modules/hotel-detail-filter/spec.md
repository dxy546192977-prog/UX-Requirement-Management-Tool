# 04 筛选（`hotel-detail-filter`）

- **供稿序号**：04  
- **预览**：[`example.html`](example.html)（嵌入父页加 `?embed=1&component=…`；顺序与间距见 [page-frame.md](../../page-frame.md)）  
- **实现说明**：以 `example.html` 为真相源；样式与结构来自设计供稿包。  

## URL 参数

- `mode=regular`：正文内常规高度。  
- `mode=sticky`：吸顶条高度。  
- `component=`：与父页 `data-component` / `postMessage` 的 `key` 一致（如 `filter` / `filter-sticky`）。  

## 变更记录

| 日期 | 说明 |
|------|------|
| 2026-04-06 | 迁入仓库。 |
