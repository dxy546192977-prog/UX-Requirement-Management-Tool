# 模块：租车 LIST · 二级导航

## 用途

**750×88** 白底：返回、取还信息块、编辑、**182×72** 黄边搜车型胶囊、仅 **更多**。

## 关键尺寸

| 元素 | 尺寸 / 位置 |
|------|-------------|
| 返回 | 48×48，`left: 24px`，垂直居中 |
| 信息区 | 约 370×72，`left: 84px`，`top: 8px`，圆角 12 |
| 搜索框 | **182×72**，`right: 96px`，`top: 8px`，边 **2px #FFE033**，圆角 36 |
| 更多 | 48×48，`right: 24px` |

## 资源

见 `example.html` 内 icon CDN（搜索、更多、编辑、时间箭头）。

## 显隐

- **不**展示收藏、分享。
- **不**单独展示双城标题「杭州-苏州」类（与框架一致）。
