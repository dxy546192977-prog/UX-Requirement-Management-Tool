# 模块：租车 LIST · 大筛选（服务卡）

## 用途

**750×113** 白底横向服务卡轨；内层卡片行 **914px** 宽，**超出裁切**。

## 单卡

- **215×89**，`padding: 12px 24px`，圆角 **12px**
- 背景：`radial-gradient(100% 100% at 100% 0%, #FFFFFF 0%, #F7F8FA 100%)`
- 标题 **24px** 字重 **500** `#0F131A`；描述 **20px** `#919499`

## 装饰图

三张 **90×60** 绝对定位在容器顶，与供稿 left 值一致；资源见 `example.html`。
