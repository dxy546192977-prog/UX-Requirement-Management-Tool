---
description: 解析或拆解 PRD 时必须读取并应用 idea、ceo、UXdesign-buff 三技能后再输出结构化简报
alwaysApply: true
---

# PRD 解析与三技能拆解（强制）

当用户任务涉及以下任一情形时，视为 **PRD 解析任务**，在完成交付前必须串联三技能（不可只跑 `prd-recognition` 默认步骤而跳过本节）：

- 解析 / 拆解 / 识别 PRD、产品需求文档、语雀需求
- 需求拆解为页面或模块、生成 AI 可执行简报
- 用户明确提到 `prd-recognition` 或准备 `fliggy-design-gui` 输入

## 1. 加载三技能

在产出结构化结果之前，使用 `Read` 读取本仓库中的三份技能说明（路径固定）：

- `.agents/skills/idea/SKILL.md`
- `.agents/skills/ceo/SKILL.md`
- `.agents/skills/uxdesign-buff/SKILL.md`

若某路径缺失，在回复中说明，并仍按各 skill 文档中的原则与语气尽力覆盖对应章节。

## 2. 输出中必须包含的独立分节

在同一份 Markdown（或同一条长回复中的清晰分节）中，**在**「页面 / 模块清单与 fliggy-design-gui 执行指令」**之前或紧接摘要之后**，依次给出：

1. **发散拆解（idea）**：按 idea skill 的流程与纪律，扩展多种方向、关键假设、取舍与待验证点；对 PRD 中模糊处列出待确认项或等价于二次加压的澄清问题。
2. **商业与战略压测（ceo）**：按 ceo skill 的视角，审视价值主张、优先级、风险与机会；区分「必须澄清」「建议上线前验证」与「可选优化」。
3. **体验与设计评审视角（UXdesign-buff）**：基于 PRD 文本还原场景与用户路径，指出链路、信息架构与体验上的风险；若用户提供 Figma 或可用 MCP，按该 skill 的证据优先级取证后再写评审要点。

然后再输出 `skills/prd-recognition` 所规定的 **需求摘要、涉及页面、模块级变更与可直接下发的执行指令**（并显式提及下游 `fliggy-design-gui`）。

## 3. 完成标准

若最终交付中缺少上述 **发散拆解 / 商业与战略压测 / 体验与设计评审** 中任一分节，则视为 **PRD 解析未完成**，须补全后再结束。
